This audio sample contains multiple languages. Identify each language spoken and provide the ISO 639-1 (or ISO 639-3 if unavailable) identifier for each. Return the result as JSON in the following format:

```json
{
  "languages": [
    {"code": "en", "name": "English", "approximate_percentage": 60},
    {"code": "es", "name": "Spanish", "approximate_percentage": 40}
  ]
}
```
